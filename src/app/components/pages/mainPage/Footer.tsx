import React from 'react';
import {StyledFooter} from "./styles/styledFooter";

const Footer = () => {
    return (
        <StyledFooter>
            <span className='footerTitle'>by Egor Dupanov (Bostil Support)</span>
        </StyledFooter>
    );
};

export default Footer;